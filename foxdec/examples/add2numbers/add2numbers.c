#include <stdio.h>

int sum;

int main() {
    int a = 5;
    int b = 3;
    sum = a + b;
    printf("The sum of %d and %d is %d\n", a, b, sum); // Print the result
    return sum; // Return the sum of a and b
}