FOXDEC DEMO


This demo runs FoxDec on the "grader" example provided by Raytheon BBN.


To run and install:

1.) Download and install Docker:

    https://www.docker.com

2.) Build the Docker image:

    docker build -t foxdec .

3.) Run the Docker image:

    docker run --rm -v ${PWD}/artifacts:/artifacts -it foxdec


Directory ./artifacts should now have been populated with information produced by FoxDec.
